apiVersion: v1
kind: Service
metadata:
  name: agent-course
  labels:
    app: agent-course
spec:
  type: ClusterIP
  ports:
    - port: 8888
      targetPort: 8888
      protocol: TCP
      name: http
  selector:
    app: agent-course