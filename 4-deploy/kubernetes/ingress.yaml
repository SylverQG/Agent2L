# Optional: Enable this ingress if you have an ingress controller installed.
# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: agent-course
#   annotations:
#     nginx.ingress.kubernetes.io/rewrite-target: /
# spec:
#   ingressClassName: nginx
#   rules:
#     - host: agent-course.local
#       http:
#         paths:
#           - path: /
#             pathType: Prefix
#             backend:
#               service:
#                 name: agent-course
#                 port:
#                   number: 8888